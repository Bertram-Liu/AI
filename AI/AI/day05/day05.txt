1. 队列和线程
                  计算          IO操作
                CPU操作计算
                真正的多线程 并行
                队列 文件的改善(tfrecords)

2. 文件读取
    读取文件: 默认读取: 一个样本
      csv: 一行
      二进制: 指定一个样本的bytes
      图片: 一张一张的读取
      csv文件
       1) 先找到文件, 构造一个列表
       2) 构造文件队列
       3) 构造阅读器, 读取队列内容
       4) 解码内容
       5) 批处理

      图片文件:
        输入: 特征值, 目标值
          图片: 像素组成
            黑白: (单通道)
              灰度值[0~255]
            彩色: (三通道)
              RGB (一个像素点有三个值组成)
        3-D张量 (长, 宽, 通道)
        所有的图片统一特征的数量 (像素值一样)
          缩放图片大小
        存储:
          unit8 (节约内存)
        读取:
          floate (提高精度)

      二进制文件:

      TFRecords文件
        tensorflow内置文件格式
          Example协议块 (类字典)
        优点:
          读取速度快, 方便移动
        1) 建立TFRecord存储器
        2) 构造example协议块
          tf.train.Example(features=tf.train.Features)
















3. 图片处理
